youmebdesign
nicky.chen@youmeb.com
nickygit2


git 安裝專案
-------
git clone https://github.com/YouMeb/Smart.git

git add .
git commit -a -m "add a html"
git pull
git push


一開始打開git
cd \d 
cd git_project\smart


git 平常使用  每做一小段就做這步
-------git
git add .
git commit -a -m 'test'
git status 
(這個做檢查  要全部都出現白字才可以做下一步)
git pull 
git push 




node操作

1. 切換到目錄, 例如 cd /d d:\git_project\smart
2. 輸入 node app
3. 看到 Express server listening on port 3000代表成功
4. 瀏覽器輸入http://127.0.0.1:3000/即可 

5. 若要新增檔案, 在資料夾中編輯 app檔案
6. 新增後在node視窗按crtl-c中斷, 在執行一次node app
